# Calibre V11 — Matchroom and transfer spotlight

## Added
- Featured Matchroom in every competition category.
- Pregame tactical analysis, match signals and account-gated fixture discussion modal.
- API-ready next-fixture handoff using the existing `/api/football` bridge.
- Persistent local post-match thread prototype using browser storage.
- Transfer-window System Fit spotlight with Anthony Gordon → Barcelona scenario seed.
- Multi-position pitch map and one-click handoff into the full fit report.

## Important
The fallback fixtures and Gordon move are intentionally labelled snapshot/scenario models. When the API and transfer-news enrichment layer are connected, live records can replace these seed records without rebuilding the UI.
